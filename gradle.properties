org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
kotlin.code.style=official
# Apple targets are declared for publishing; on non-macOS hosts the Kotlin
# Gradle Plugin disables them. Keep the build quiet about it.
kotlin.native.ignoreDisabledTargets=true
